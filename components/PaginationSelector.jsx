import React from 'react';

export default class PaginationSelector extends React.Component {
  render() {
    const { numMovies } = this.props;

    <div>`Displaying ${numMovies} per page`</div>;
  }
}
